<%- include('../layouts/main', { 
  title: 'Filmer',
  page: 'movies',
  body: function() { 
    return include('../partials/movies-content', { 
      movies: movies, 
      search: search, 
      genres: genres,
      currentPage: currentPage,
      totalPages: totalPages,
      selectedGenre: selectedGenre,
      selectedSort: selectedSort,
      error: error
    });
  }
}) %>
