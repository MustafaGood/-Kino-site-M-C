<%- include('../layouts/main', { body: `
<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 text-center">
            <div class="mb-5">
                <i class="fas fa-exclamation-circle text-danger" style="font-size: 6rem;"></i>
            </div>
            <h1 class="display-4 fw-bold text-danger mb-4">Oops!</h1>
            <h2 class="h3 mb-4">Något gick fel</h2>
            <p class="lead text-muted mb-4">
                Vi ber om ursäkt, men något gick fel på vår sida. 
                Vårt team har blivit notifierat och arbetar på att lösa problemet.
            </p>
            
            <% if (error && error.message) { %>
                <div class="alert alert-danger text-start mb-4">
                    <strong>Felmeddelande:</strong> <%= error.message %>
                </div>
            <% } %>
            
            <% if (stack && process.env.NODE_ENV !== 'production') { %>
                <div class="alert alert-warning text-start mb-4">
                    <strong>Stack trace (endast utveckling):</strong>
                    <pre class="mt-2"><%= stack %></pre>
                </div>
            <% } %>
            
            <div class="d-flex justify-content-center gap-3">
                <a href="/" class="btn btn-primary btn-lg">
                    <i class="fas fa-home me-2"></i>
                    Tillbaka till startsidan
                </a>
                <button onclick="window.history.back()" class="btn btn-outline-primary btn-lg">
                    <i class="fas fa-arrow-left me-2"></i>
                    Tillbaka
                </button>
            </div>
        </div>
    </div>
</div>

<div class="bg-light py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <h3 class="mb-4">Rapportera problemet</h3>
                <p class="text-muted mb-4">
                    Om problemet kvarstår, vänligen kontakta oss med information om vad du försökte göra.
                </p>
                <div class="row">
                    <div class="col-md-6 mb-3">
                        <div class="card border-0 bg-white">
                            <div class="card-body">
                                <i class="fas fa-envelope text-primary fa-2x mb-3"></i>
                                <h5>Email Support</h5>
                                <p class="text-muted">support@kino-site.se</p>
                            </div>
                        </div>
                    </div>
                    <div class="col-md-6 mb-3">
                        <div class="card border-0 bg-white">
                            <div class="card-body">
                                <i class="fas fa-phone text-info fa-2x mb-3"></i>
                                <h5>Telefonsupport</h5>
                                <p class="text-muted">08-123 45 67</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
` }) %>
